@{
	ViewData["Title"] = "Concat";
}
<!-- contact section -->
<section class="contact_section layout_padding">
    <div class="container">

        <div class="heading_container">
            <h2>
                Bizimle İletişime Geçin
            </h2>
        </div>
        <div class="">
            <div class="">
                <div class="row">
                    <div class="col-md-9 mx-auto">
                        <div class="bg-success">
                            @if (ViewData["contactvalue"] != null)
                            {
                                <p>@ViewData["contactvalue"]</p>
                            }
                        </div>
                        <div class="contact-form">
                            <form method="post">
                                <div>
                                    <input type="text" placeholder="Adınız Soyadınız" name="Adsoyad" itemid="Adsoyad" required maxlength="35" autofocus>
                                </div>
                                <div>
                                    <input type="tel" placeholder="Telefon Numaranız örn(5xx xxx xx xx)" name="Telno" itemid="Telno" required maxlength="10">
                                </div>
                                <div>
                                    <input type="email" placeholder="Email Adresiniz" name="Email" itemid="Email" required maxlength="50">
                                </div>

                                <div>
                                    <select name="Department" itemid="Department" required class="form-control rounded-pill border-5 show-tick" style="padding:5px 20px 5px 15px" >
                                        <option value="Destek">Destek</option>
                                        <option value="Satış">Satış</option>
                                        <option value="Geliştirici">Geliştirici</option>
                                    </select>
                                </div>

                                <div>
                                    <textarea placeholder="Message" class="input_message" name="Message" itemid="Message" required></textarea>

                                </div>
                                <div class="d-flex justify-content-center">
                                    <button type="submit" class="btn_on-hover" name="Gonder" itemid="Gonder">
                                        Gönder
                                    </button>
                                </div>
                            </form>
                        </div>
                      
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>


<!-- end contact section -->
